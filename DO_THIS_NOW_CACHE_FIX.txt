================================================================================
                   DO THIS NOW - CACHE FIX (5 Minutes)
================================================================================

STATUS: Code IS fixed, but Python cached old version
SOLUTION: Clear cache + restart with no-cache flags
TIME: 5 minutes
RISK: None

================================================================================
                        IMMEDIATE ACTION
================================================================================

STEP 1: STOP THE SERVER
   In your terminal, press: Ctrl+C
   
   Then wait 5 seconds for it to fully stop.

STEP 2: KILL PYTHON PROCESSES
   
   Open PowerShell and copy-paste this:
   
   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   Get-Process python -ErrorAction SilentlyContinue | Stop-Process -Force
   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STEP 3: CLEAR ALL CACHE
   
   In PowerShell, copy-paste this entire block:
   
   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   cd e:\login
   Get-ChildItem -Path . -Include "__pycache__" -Recurse -Force | Remove-Item -Recurse -Force
   Get-ChildItem -Path . -Include "*.pyc" -Recurse -Force | Remove-Item -Force
   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

   Wait 2 seconds.

STEP 4: START FRESH SERVER
   
   Copy-paste this:
   
   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   cd e:\login\auth_project
   python -Bc manage.py runserver
   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

   The -Bc flags force Python to NOT use cached bytecode.

STEP 5: WAIT FOR THIS MESSAGE
   
   You should see in the terminal:
   
   ✅ Real-time signal handlers registered successfully
   
   If you see this, the fix worked!

STEP 6: TEST IN BROWSER
   
   Visit: http://localhost:8000/post-project/
   
   Fill in the form and submit.
   
   EXPECTED: Project created with NO error ✅

================================================================================
                          CONSOLIDATED COMMAND
================================================================================

If you want to do it all in one go, here's the complete script:

Copy-paste this entire block into PowerShell:

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
cd e:\login
Get-Process python -ErrorAction SilentlyContinue | Stop-Process -Force
Start-Sleep -Seconds 2
Get-ChildItem -Path . -Include "__pycache__" -Recurse -Force | Remove-Item -Recurse -Force
Get-ChildItem -Path . -Include "*.pyc" -Recurse -Force | Remove-Item -Force
cd auth_project
python -Bc manage.py runserver
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Then test: http://localhost:8000/post-project/

================================================================================
                         VERIFICATION
================================================================================

After running the commands above, verify:

1. Terminal shows:
   "Real-time signal handlers registered successfully"

2. Project creation works without error

3. No AttributeError in logs

If all three = SUCCESS ✅

================================================================================
                         WHY THIS WORKS
================================================================================

The problem: Python cached old compiled bytecode (.pyc files)

Solution: Delete .pyc files + run with -Bc flags

Result: Python recompiles fresh from the fixed source code

================================================================================
                       IF STILL NOT WORKING
================================================================================

Check if the code was actually fixed by running:

findstr "actor=instance.user" auth_project\accounts\signals_realtime.py

Should return results with "instance.user" (not "instance.owner")

If it returns "instance.owner":
   - The code fix didn't apply
   - I'll manually provide the corrected code to paste

================================================================================
                          SUMMARY
================================================================================

What to do:
   1. Stop server (Ctrl+C)
   2. Kill Python processes (PowerShell command)
   3. Delete cache (PowerShell command)
   4. Restart with -Bc flags (python -Bc manage.py runserver)
   5. Test (http://localhost:8000/post-project/)

Expected result:
   - No AttributeError
   - Project created successfully

Time needed:
   5 minutes

Risk level:
   NONE (just clearing cache)

================================================================================

                    RUN THE COMMANDS IN STEP 1-4 NOW!

                    Then let me know if it works! ✅

================================================================================
